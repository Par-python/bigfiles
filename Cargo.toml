[package]
name = "bigfiles"
version = "0.1.0"
edition = "2021"

[dependencies]
walkdir    = "2"
clap       = { version = "4", features = ["derive"] }
owo-colors = "4"
serde      = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
lto = true
codegen-units = 1
